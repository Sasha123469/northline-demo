# Northline Studio Demo

Two-page responsive website.

The contact form uses browser localStorage as a lightweight demo data layer. For a production project, this layer can be replaced by Supabase, Firebase, or a custom backend/database.
